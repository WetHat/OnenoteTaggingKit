<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:scm="clr-namespace:System.ComponentModel;assembly=WindowsBase"
        xmlns:local="clr-namespace:WetHatLab.OneNote.TaggingKit.edit"
        xmlns:prop="clr-namespace:WetHatLab.OneNote.TaggingKit.Properties"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:TaggingKit="clr-namespace:WetHatLab.OneNote.TaggingKit"
        x:Class="WetHatLab.OneNote.TaggingKit.edit.TagEditor"
        Height="{Binding Source={x:Static prop:Settings.Default},Path=TagEditorWindow_Height,Mode=TwoWay}"
        Width="{Binding Source={x:Static prop:Settings.Default},Path=TagEditorWindow_Width,Mode=TwoWay}"
        Left="{Binding Source={x:Static prop:Settings.Default},Path=TagEditorWindow_Left,Mode=TwoWay}"
        Top="{Binding Source={x:Static prop:Settings.Default},Path=TagEditorWindow_Top,Mode=TwoWay}"
        MinWidth="200"
        MinHeight="350"
        Name="tagEditorWindow"
        mc:Ignorable="d"
        ShowInTaskbar="True"
        d:DataContext="{d:DesignInstance IsDesignTimeCreatable=True, Type={x:Type local:TagEditorDesignerModel}}"
        WindowStyle="ToolWindow"
        Title="{Binding PageTitle,Mode=OneWay}"
        Icon="/OneNoteTaggingKit;component/Resources/tag 32x32.png"
        Loaded="Window_Loaded" Closing="editTags_Closing">
    <Window.Resources>
        <CollectionViewSource Source="{Binding KnownTags}" x:Key="knownTags">
            <CollectionViewSource.SortDescriptions>
                <scm:SortDescription Direction="Ascending" />
            </CollectionViewSource.SortDescriptions>
        </CollectionViewSource>

    </Window.Resources>
    <Window.Background>
        <LinearGradientBrush EndPoint="0.5,1" MappingMode="RelativeToBoundingBox" StartPoint="0.5,0">
            <GradientStop Color="Ivory" Offset="0.497"/>
            <GradientStop Color="#FFE1ECF7" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" MinHeight="100"/>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="2*" MinHeight="100" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <GroupBox Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  Margin="5,0,5,0"
                  Background="White"
                  Header="{Binding Mode=OneTime, Source={x:Static prop:Resources.TagEditor_Tags_GroupBox_Title}}"
                  ToolTip="{Binding Mode=OneTime, Source={x:Static prop:Resources.TagEditor_Tags_GroupBox_ToolTip}}">
            <local:SimpleTagsPanel Tags="{Binding PageTags}" VerticalAlignment="Stretch"/>
        </GroupBox>

        <ProgressBar Name="pBar"
                     Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3"
                     IsIndeterminate="True"
                     Width="100" Height="20"
                     HorizontalAlignment="Center" VerticalAlignment="Center"
                     Foreground="LightBlue" Background="WhiteSmoke"/>
        <Button Grid.Row="0" Grid.ColumnSpan="2"
                Width="26"
                Margin="0,0,25,0"
                Background="Transparent"
                VerticalAlignment="Top" HorizontalAlignment="Right"
                BorderThickness="0" BorderBrush="LightBlue"
                ToolTip="{Binding Mode=OneTime, Source={x:Static prop:Resources.TagEditor_Tag_RefreshButton_Tooltip}}"
                Click="RefreshButton_Click" >
            <Image Source="/OneNoteTaggingKit;component/Resources/refresh-32.png"/>
        </Button>
        <ComboBox Name="tagComboBox" Grid.Row="1" Grid.Column="0"
                  Margin="5,5,0,10"
			    HorizontalAlignment="Stretch" VerticalAlignment="Center"
			    IsEditable="True" StaysOpenOnEdit="True"
			    IsTextSearchEnabled="True"
			    ToolTip="{Binding Mode=OneTime, Source={x:Static prop:Resources.TagEditor_Tag_ComboBox_Tooltip}}"
			    ItemsSource="{Binding Source={StaticResource ResourceKey=knownTags}}" KeyUp="TagDropDown_KeyUp"/>
        <Button Grid.Row="1" Grid.Column="1"
                Background="Transparent"
                Height="26"
                BorderBrush="LightBlue"
                ToolTip="{Binding Mode=OneTime, Source={x:Static prop:Resources.TagEditor_Tag_AddButton_Tooltip}}"
                Click="AddTagsToPageButton_Click" Margin="5,5,5,10">
            <Image Source="/OneNoteTaggingKit;component/Resources/Add Tag 26.png"/>
        </Button>
        <GridSplitter Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3"
                      ResizeBehavior="PreviousAndNext"
                      ResizeDirection="Rows"
                      VerticalAlignment="Bottom"
                      HorizontalAlignment="Stretch"
                      Height="5" >
            <GridSplitter.Background>
                <LinearGradientBrush EndPoint="0.5,1" MappingMode="RelativeToBoundingBox" StartPoint="0.5,0">
                    <GradientStop Color="Black" Offset="1"/>
                    <GradientStop Color="#FFE2EDF7" Offset="0.67"/>
                </LinearGradientBrush>
            </GridSplitter.Background>
        </GridSplitter>
        <GroupBox Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3" 
                  HorizontalAlignment="Stretch" 
                  Margin="5,0,5,0" Background="Transparent"
                  ToolTip="{Binding Mode=OneTime, Source={x:Static prop:Resources.TagEditor_Pages_GroupBox_Tooltip}}"
                  Header="{Binding Mode=OneTime, Source={x:Static prop:Resources.TagEditor_Pages_GroupBox_Title}}">
            <ListBox Background="Transparent"
                     BorderThickness="0"
                     ItemsSource="{Binding Pages, Mode=OneWay}"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Width="1">
                            <TextBlock >
                          
                            <!--
                            <Hyperlink Tag="{Binding ID, Mode=OneWay}"
                                       Click="Page_Hyperlink_Click"
                                       ToolTip="{Binding HitHighlightedTooltip, Mode=OneWay}">
                                <ContentControl Content="{Binding HitHighlightedTitle, Mode=OneWay}"/>
                            </Hyperlink>
                            -->
                            </TextBlock>
                        </Border>

                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </GroupBox>
    </Grid>
</Window>
